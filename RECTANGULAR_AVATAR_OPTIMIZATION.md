# 角色卡片使用长方形头像优化

## 完成的改进

### 1. 使用长方形头像（avatarRtUrl）

**之前**：使用正方形头像 `avatarSqUrl`

```tsx
<img src={char.charData.avatarSqUrl} className="aspect-square" />
```

**现在**：优先使用长方形头像，回退到正方形

```tsx
<img
  src={char.charData.avatarRtUrl || char.charData.avatarSqUrl}
  className="h-32"
/>
```

**优势**：

- ✅ 长方形头像更适合角色立绘展示
- ✅ 显示更多角色细节
- ✅ 回退机制确保兼容性

---

### 2. 增大卡片尺寸

**之前**：`min-h-[200px]`

**现在**：`min-h-[280px]`

增加了 **40%** 的高度，让内容更舒展。

---

### 3. 增大标题字体

**之前**：默认字体大小

**现在**：`text-lg`（大一号）

更清晰的视觉层次。

---

### 4. 固定图片高度

**之前**：`aspect-square`（正方形，宽高比 1:1）

**现在**：`h-32`（固定高度 128px，宽度自适应）

**效果**：

- 长方形头像完整显示
- 三列布局保持整齐
- 空槽位也使用相同高度

---

### 5. 增强渐变背景

**之前**：`from-black/70`（70% 透明度）

**现在**：`from-black/80`（80% 透明度）

文字在复杂背景下更清晰可读。

---

## 视觉效果对比

### 之前（正方形头像）

```
┌──────────────────────────────┐
│ Characters (2/3)             │
├──────────────────────────────┤
│ ┌──────┐ ┌──────┐ ┌──────┐  │
│ │      │ │      │ │Empty │  │
│ │ Sq   │ │ Sq   │ │      │  │
│ │头像  │ │头像  │ │      │  │
│ │Name  │ │Name  │ │      │  │
│ └──────┘ └──────┘ └──────┘  │
└──────────────────────────────┘
     200px 高度
```

### 现在（长方形头像）

```
┌──────────────────────────────┐
│ Characters (2/3)             │ ← text-lg
├──────────────────────────────┤
│ ┌──────────┐ ┌──────────┐   │
│ │          │ │          │   │
│ │ Rt 头像  │ │ Rt 头像  │ E │
│ │          │ │          │ m │
│ │          │ │          │ p │
│ │          │ │          │ t │
│ │Name      │ │Name      │ y │
│ └──────────┘ └──────────┘   │
└──────────────────────────────┘
     280px 高度 (+40%)
     h-32 (128px) 图片高度
```

---

## 技术要点

### 1. 回退机制

```tsx
src={char.charData.avatarRtUrl || char.charData.avatarSqUrl}
```

如果 `avatarRtUrl` 不存在，自动使用 `avatarSqUrl`，确保不会显示空白。

### 2. 固定高度 vs 宽高比

```css
/* ❌ 之前：强制正方形 */
aspect-square

/* ✅ 现在：固定高度，宽度自适应 */
h-32
```

长方形头像通常是 2:1 或 3:2 的宽高比，固定高度可以让它们自然显示。

### 3. 空槽位高度一致

```tsx
className = "h-32 rounded-lg ...";
```

空槽位也使用 `h-32`，与有内容的槽位保持高度一致，避免布局跳动。

### 4. 渐变背景增强

```css
bg-gradient-to-t from-black/80 to-transparent
```

- `from-black/80` - 底部 80% 透明度的黑色
- `to-transparent` - 顶部完全透明
- 更高的不透明度确保文字在任何背景下都清晰

---

## JSON 数据结构

```json
{
  "charData": {
    "id": "chr_0027_tangtang",
    "name": "涤火杰西卡",
    "avatarSqUrl": "https://.../square.png", // 正方形头像
    "avatarRtUrl": "https://.../rectangle.png", // 长方形头像 ⭐
    "rarity": { "key": "rarity_6", "value": "6" },
    "profession": { "key": "profession_assault", "value": "突击" }
  }
}
```

**字段说明**：

- `avatarSqUrl` - Square（正方形），通常用于列表、小图标
- `avatarRtUrl` - Rectangle（长方形），通常用于卡片、详情页

---

## 相关文件

- `src/components/cards/character-list-card.tsx` - 卡片组件

## 优势总结

| 特性       | 之前          | 现在    | 提升      |
| ---------- | ------------- | ------- | --------- |
| 头像形状   | 正方形        | 长方形  | ✅ 更美观 |
| 卡片高度   | 200px         | 280px   | +40%      |
| 图片高度   | aspect-square | 128px   | ✅ 更清晰 |
| 标题字体   | 默认          | text-lg | ✅ 更醒目 |
| 渐变透明度 | 70%           | 80%     | ✅ 更清晰 |
| 回退机制   | 无            | 有      | ✅ 更可靠 |

---

## 最佳实践

**规则 1**：优先使用更适合展示的媒体资源（长方形 vs 正方形）。

**规则 2**：始终提供回退机制，确保兼容性。

**规则 3**：固定高度比固定宽高比更适合展示不同比例的内容。

**规则 4**：空状态应该与有内容的状态保持一致的尺寸，避免布局跳动。

**规则 5**：渐变背景的透明度应该根据内容复杂度调整，确保文字可读性。
